spring:
  application:
    name: easylearn-user
  data:
    mongodb:
      database: user
      host: localhost
server:
  port: 8080

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
    healthcheck:
      enabled: true
